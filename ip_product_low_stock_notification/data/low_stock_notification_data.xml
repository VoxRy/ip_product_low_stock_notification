<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <data noupdate="1">
    <record id="ir_cron_low_stock_notification" model="ir.cron">
      <field name="name">Low Stock Notification</field>
      <field name="model_id" ref="stock.model_stock_quant"/>
      <field name="state">code</field>
      <!-- CDATA kullan: XML’de kaçış derdi kalmasın -->
      <field name="code"><![CDATA[
model.with_context(
  allowed_company_ids=[env.company.id],
  company_id=env.company.id,
  location=env.company.warehouse_id.lot_stock_id.id if env.company.warehouse_id else env.ref('stock.stock_location_stock').id,
  company_owned=True
).send_low_stock_notification()
      ]]></field>
      <field name="interval_number">24</field>
      <field name="interval_type">hours</field>
      <field name="numbercall">-1</field>
      <field name="active">True</field>
    </record>
  </data>
</odoo>
